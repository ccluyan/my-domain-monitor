name = "domain-monitor"
main = "src/worker.js"
compatibility_date = "2023-10-02"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "domain-db"
database_id = "USER_MUST_REPLACE_THIS_WITH_THEIR_OWN_ID"

# 静态资源 (如果以后需要，现在暂时不需要)
# [site]
# bucket = "./public"
